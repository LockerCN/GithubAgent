Metadata-Version: 2.4
Name: github-hot-repo-agent
Version: 0.1.0
Summary: Github 热门仓库推送 Agent 阶段一骨架
Requires-Python: <3.14,>=3.13
Description-Content-Type: text/markdown
Provides-Extra: dev
Requires-Dist: pytest<9,>=8; extra == "dev"

# Github 热门仓库推送 Agent

## Development Plan

## Version

- 文档版本：V1.0
- 产出日期：2026-04-11
- 适用范围：MVP（最小可用版）

---

## 1. 文档目标

本文档基于 `Docs/V1.0/HLD V1.0.md`、`Docs/V1.0/LLD V1.0.md` 与当前 `memory-bank`，将设计基线转化为可执行的开发计划，用于指导 V1.0 的项目搭建、模块实现、测试验证与交付验收。

本文档重点回答以下问题：

1. V1.0 应按什么顺序开发。
2. 每个阶段需要完成哪些工作。
3. 每个阶段的交付物和验收标准是什么。
4. 当前 MVP 的关键风险应如何控制。

---

## 2. 计划基线

### 2.1 目标基线

V1.0 的目标是在 `GitHub Actions` 中实现每日自动运行，由 Agent 自主完成热门仓库候选获取、候选回退、历史记录查询、仓库内容读取与中文介绍生成，再由 Workflow 完成飞书发送、归档保存与状态记录。

### 2.2 范围基线

V1.0 仅覆盖以下内容：

1. `trendshift.io` `Daily Explore` 作为唯一榜单来源。
2. 基于 Python 3.13 与 Conda 环境 `Agent` 的项目实现。
3. Agent 中心化编排与 Function Calling 工具集。
4. `GitHub API` 仓库信息读取。
5. 飞书消息发送。
6. 仓库内状态与归档持久化。
7. Trendshift 抓取模拟测试、飞书发送模拟测试及必要的核心单元测试。

### 2.3 非目标基线

本计划不包含以下内容：

1. 多仓库汇总推送。
2. 后台管理界面。
3. 外部数据库与消息队列。
4. 多模型路由与复杂调度策略。
5. 高级日志平台与监控平台接入。

### 2.4 开发原则

1. 优先最简单直接的实现路径。
2. 保持 Workflow 轻量，核心业务决策交由 Agent。
3. 严格遵守 HLD 与 LLD 的模块边界。
4. 先完成最小闭环，再补齐测试与运行编排。
5. 阶段交付以“可运行、可验证、可继续迭代”为标准。

---

## 3. 总体开发策略

### 3.1 开发主线

V1.0 采用“先骨架、后能力、再闭环、最后验收”的推进方式：

1. 先建立项目结构、配置模型、异常与日志基础设施。
2. 再实现本地持久化服务与外部客户端。
3. 之后实现工具层、Agent 运行时与 Workflow。
4. 最后补齐 GitHub Actions、测试与验收文档。

### 3.2 交付原则

每个阶段都需要满足以下要求：

1. 代码目录结构完整并与 LLD 一致。
2. 关键模块可单独验证。
3. 新增功能具备最小必要测试。
4. 不引入超出 MVP 需求的工程复杂度。

### 3.3 阶段依赖

开发依赖关系如下：

1. 项目骨架先于所有功能模块。
2. 配置、异常、日志先于客户端与服务。
3. 服务与客户端先于工具层。
4. 工具层先于 Agent 运行时。
5. Agent 运行时先于 Workflow 闭环联调。
6. 业务闭环完成后再接入 GitHub Actions 与完整测试。

---

## 4. 阶段划分

### 4.1 阶段一：项目骨架与基础设施

#### 阶段目标

建立与 LLD 一致的目录结构、配置体系、通用异常与日志基础设施，为后续模块开发提供稳定骨架。

#### 主要任务

1. 初始化 `src/`、`config/`、`runtime/`、`tests/`、`.github/workflows/` 目录。
2. 创建 `config/app.example.toml` 与 `config/app.toml` 模板。
3. 实现配置数据模型与 `ConfigLoader`。
4. 实现公共异常定义。
5. 实现控制台日志初始化工具。
6. 创建 `main.py` 的最小入口骨架。

#### 交付物

1. 项目目录骨架。
2. 配置样例文件。
3. 配置加载模块。
4. 公共异常模块。
5. 日志工具模块。
6. 可执行但功能未闭环的程序入口。

#### 验收标准

1. 目录结构与 `Docs/V1.0/LLD V1.0.md` 一致。
2. 配置文件可被成功读取并映射为 `AppConfig`。
3. 配置缺失或非法时能抛出明确异常。
4. 程序入口可完成配置加载与基础日志输出。

### 4.2 阶段二：本地服务与消息能力

#### 阶段目标

完成归档、状态记录与飞书消息构建能力，为业务闭环提供本地副作用支撑。

#### 主要任务

1. 实现 `ArchiveService`。
2. 实现 `DeliveryStateService`。
3. 定义归档 Markdown 输出格式。
4. 定义状态文件 JSON 结构与读写逻辑。
5. 实现 `FeishuMessageBuilder` 的成功消息与错误消息构建能力。
6. 编写状态服务相关测试。

#### 交付物

1. 归档服务模块。
2. 状态服务模块。
3. 飞书消息构建模块。
4. 状态服务测试。

#### 验收标准

1. 空状态文件场景可正常返回空记录。
2. 成功记录可追加写入并按时间倒序读取。
3. 归档文件可按 `YYYY-MM-DD.md` 生成。
4. 成功消息与错误消息负载结构可稳定输出。

### 4.3 阶段三：外部客户端实现

#### 阶段目标

实现 Trendshift、Github、LLM、飞书四类外部访问能力，并为工具层与 Workflow 提供稳定接口。

#### 主要任务

1. 实现 `TrendshiftBrowserClient`。
2. 实现 `GithubApiClient`。
3. 实现 `LlmProviderClient` 的统一适配接口。
4. 实现 `FeishuWebhookClient`。
5. 编写 Trendshift 抓取模拟测试。
6. 编写飞书发送模拟测试。

#### 交付物

1. Trendshift 客户端。
2. Github 客户端。
3. LLM 客户端适配层。
4. 飞书 Webhook 客户端。
5. Trendshift 测试。
6. 飞书测试。

#### 验收标准

1. Trendshift 客户端能稳定提取前 `N` 个仓库候选。
2. Github 客户端能获取元信息、目录树与文件内容。
3. 文件内容超长时可按配置截断。
4. 飞书客户端能发送标准 POST 负载并对异常响应抛出明确异常。
5. LLM 客户端接口可支撑工具调用循环。

### 4.4 阶段四：工具层与 Agent 运行时

#### 阶段目标

实现 Agent 的工具注册、提示词组装、工具调用循环与结构化输出解析，使 Agent 具备自主完成核心业务流程的能力。

#### 主要任务

1. 实现 `BaseTool` 与工具抽象规范。
2. 实现五个核心工具：
   - `get_trendshift_top_repositories`
   - `get_delivery_records`
   - `get_repository_metadata`
   - `get_repository_tree`
   - `get_repository_file_content`
3. 实现 `ToolRegistry`。
4. 实现 `PromptFactory`。
5. 实现 `GithubHotRepoAgentRuntime`。
6. 实现 Agent 输出解析与校验逻辑。
7. 编写 Agent 输出解析测试。

#### 交付物

1. 工具层模块。
2. 工具注册器。
3. Agent 提示词工厂。
4. Agent 运行时。
5. Agent 输出解析测试。

#### 验收标准

1. Agent 运行时可完成“模型响应 -> 工具执行 -> 工具结果回填 -> 再次推理”的循环。
2. 工具返回值均为稳定 JSON 可序列化结构。
3. 提示词中已明确主数据源、README 必读、候选回退与最终 JSON 输出约束。
4. 非法输出可被识别并抛出 `AgentOutputParseError`。

### 4.5 阶段五：Workflow 闭环联调

#### 阶段目标

打通从入口到消息发送、归档保存、状态记录的完整业务闭环。

#### 主要任务

1. 实现 `DailyHotRepoWorkflow`。
2. 在 `main.py` 完成依赖显式装配。
3. 组装运行上下文与 `AgentRunRequest`。
4. 接入飞书发送、归档保存、状态追加。
5. 完成成功链路联调。
6. 完成错误链路联调。

#### 交付物

1. Workflow 实现。
2. 完整程序入口。
3. 端到端本地联调结果。

#### 验收标准

1. 程序可以从入口执行到 Agent 输出结果消费。
2. 成功链路可完成消息构建、发送、归档与状态写入。
3. 失败链路可输出错误日志并尝试发送飞书告警。
4. Workflow 不包含候选遍历与文件选择等 Agent 内部决策逻辑。

### 4.6 阶段六：GitHub Actions 与交付验收

#### 阶段目标

完成自动化运行配置、关键测试补齐与 V1.0 交付验收。

#### 主要任务

1. 编写 `.github/workflows/daily_hot_repo.yml`。
2. 配置 Conda 环境 `Agent` 与 Python 3.13。
3. 配置依赖安装与主程序执行命令。
4. 处理运行所需仓库写权限。
5. 补齐测试执行入口。
6. 完成一次完整演练与交付检查。

#### 交付物

1. GitHub Actions 工作流文件。
2. 可执行测试命令。
3. V1.0 验收记录。

#### 验收标准

1. GitHub Actions 能按计划触发主程序。
2. Workflow 能在自动化环境完成最小闭环运行。
3. 关键测试可在本地或 CI 中执行。
4. 仓库具备归档与状态回写所需权限配置。

---

## 5. 工作分解结构

### 5.1 模块级任务清单

1. `config`
   - 配置模型、配置样例、配置加载与校验。
2. `common`
   - 公共异常、日志初始化。
3. `models`
   - 仓库模型、推送模型、Agent 模型、配置模型。
4. `services`
   - 归档服务、状态服务、飞书消息构建。
5. `clients`
   - Trendshift、Github、LLM、Feishu。
6. `tools`
   - 五个核心工具与基础抽象。
7. `agent`
   - PromptFactory、ToolRegistry、AgentRuntime。
8. `workflows`
   - DailyHotRepoWorkflow。
9. `entry`
   - `src/main.py` 组合根。
10. `ops`
   - GitHub Actions、运行目录、配置模板。
11. `tests`
   - Trendshift、Feishu、状态服务、Agent 输出解析。

### 5.2 优先级排序

1. `P0`
   - 配置加载、异常定义、状态服务、归档服务、飞书消息构建。
2. `P1`
   - Trendshift 客户端、飞书客户端、Github 客户端。
3. `P1`
   - 工具层、ToolRegistry、PromptFactory、AgentRuntime。
4. `P2`
   - Workflow 联调、主程序入口、GitHub Actions。
5. `P2`
   - 测试补齐与交付验收。

---

## 6. 里程碑计划

### 6.1 里程碑定义

1. `M1：骨架完成`
   - 项目目录、配置模型、异常与日志基础设施完成。
2. `M2：基础能力完成`
   - 本地服务与核心外部客户端完成。
3. `M3：Agent 能力完成`
   - 工具层、提示词、Agent 运行时完成。
4. `M4：业务闭环完成`
   - Workflow 可打通成功链路与失败链路。
5. `M5：交付完成`
   - GitHub Actions、关键测试与验收资料完成。

### 6.2 里程碑通过标准

1. `M1` 通过后，代码可完成启动与配置校验。
2. `M2` 通过后，外部访问能力与本地持久化能力可单独验证。
3. `M3` 通过后，Agent 可在本地完成工具调用循环。
4. `M4` 通过后，系统具备完整业务闭环。
5. `M5` 通过后，V1.0 具备上线试运行条件。

---

## 7. 测试与验收计划

### 7.1 必测范围

1. Trendshift 抓取模拟测试。
2. 飞书发送模拟测试。
3. `DeliveryStateService` 读写测试。
4. Agent 输出解析测试。

### 7.2 联调范围

1. Agent 正常选仓与输出链路。
2. 候选仓库失败后的回退链路。
3. 飞书发送成功后的归档与状态写入链路。
4. Agent 或客户端异常时的告警链路。

### 7.3 V1.0 完成定义

满足以下条件即可视为 V1.0 完成：

1. 能在 `GitHub Actions` 中定时触发。
2. Agent 能自主完成候选获取、候选回退、仓库读取与中文介绍生成。
3. Workflow 能完成飞书发送、归档保存与状态记录。
4. 关键测试已落地并可执行。
5. 文档、代码结构与实际实现保持一致。

---

## 8. 风险与应对

### 8.1 主要风险

1. `trendshift.io` 页面结构变化导致抓取失败。
2. Agent 提示词或工具描述不充分，导致自主调用路径偏移。
3. Github 文件读取量过大，影响模型上下文与执行稳定性。
4. GitHub Actions 写权限不足，导致状态与归档无法回写。
5. 飞书发送成功后持久化失败，导致结果不一致。

### 8.2 应对策略

1. 将 Trendshift 解析逻辑集中在单一客户端，便于快速修复。
2. 在提示词中明确主数据源、回退规则、README 必读与输出格式。
3. 通过 `tree_max_entries` 与 `file_max_chars` 限制读取规模。
4. 在工作流中显式配置写权限，并在联调阶段优先验证回写动作。
5. 将发送、归档、状态写入分别记录日志，便于故障定位。

---

## 9. 推荐实施顺序

建议按以下顺序执行开发：

1. 完成项目骨架、配置、异常与日志。
2. 完成状态服务、归档服务与飞书消息构建。
3. 完成飞书客户端与 Trendshift 客户端，并优先补齐测试。
4. 完成 Github 客户端与 LLM 客户端适配层。
5. 完成工具层、ToolRegistry、PromptFactory 与 AgentRuntime。
6. 完成 Workflow、`main.py` 与本地闭环联调。
7. 完成 GitHub Actions、自动化运行验证与最终验收。

---

## 10. 结论

V1.0 的开发应围绕“最小闭环优先、Agent 中心化、外围副作用可控”三条主线推进。

在执行层面，最合理的路径是先落基础设施，再落服务与客户端，随后完成 Agent 核心链路，最后打通 Workflow 与 GitHub Actions。按本计划推进后，项目可以以较低工程复杂度完成 MVP 交付，并为后续版本演进保留清晰的扩展边界。
